
import { CourseConfig } from '../types';

const JUNIOR_MODULES = [
  {
    id: 'tj1', title: '1. Основы HCL и Workflow', desc: 'Sytax, CLI, Providers',
    questions: [
      {
        q: "Что такое Terraform State и зачем он нужен?",
        a: `<p><strong>State (tfstate)</strong> — это база данных, которая маппит (сопоставляет) вашу конфигурацию в файлах (<code>.tf</code>) с реальными ресурсами в облаке (ID инстансов, IP адреса).</p>
            <p><strong>Зачем нужен:</strong></p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Mapping:</strong> Terraform знает, что ресурс <code>aws_instance.web</code> соответствует реальному ID <code>i-1234567890</code>.</li>
                <li><strong>Metadata:</strong> Хранит зависимости между ресурсами, чтобы удалять их в правильном порядке.</li>
                <li><strong>Performance:</strong> Кэширует атрибуты ресурсов, чтобы не опрашивать облако каждый раз (хотя <code>refresh</code> это делает).</li>
                <li><strong>Syncing:</strong> Блокировка (Locking) предотвращает одновременное изменение инфраструктуры двумя людьми.</li>
            </ul>`,
        tip: "Файл состояния содержит данные в открытом виде (включая пароли и ключи!). Никогда не коммитьте `terraform.tfstate` в Git."
      },
      {
        q: "В чем разница между `terraform plan` и `terraform apply`?",
        a: `<p>Это два этапа одного процесса:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Plan:</strong> Это "Read-only" операция. Terraform сверяет код с текущим State (и иногда с реальным облаком), вычисляет разницу (Diff) и показывает, что <em>будет</em> сделано. Никаких изменений в облаке не происходит.</li>
                <li><strong>Apply:</strong> Выполняет изменения. Сначала он запускает неявный <code>plan</code>, просит подтверждение, и затем делает API вызовы к провайдеру для создания/удаления ресурсов. После успеха обновляет State файл.</li>
            </ul>`,
        tip: "Хорошей практикой в CI/CD является сохранение плана в файл: `terraform plan -out=tfplan`. Это гарантирует, что `apply` выполнит именно то, что вы увидели на этапе ревью, даже если инфраструктура изменилась за это время."
      },
      {
        q: "Как работает блок `provider`?",
        a: `<p>Terraform — это ядро, которое ничего не знает об облаках. Всю работу делают <strong>Провайдеры</strong> (плагины).</p>
            <p>Блок <code>provider "aws" { ... }</code> сообщает Terraform:</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li>Какой плагин скачать (из Terraform Registry).</li>
                <li>Как аутентифицироваться (region, access keys, profile).</li>
            </ol>
            <p>Без провайдера Terraform не может управлять ни одним ресурсом.</p>`,
        tip: "Можно использовать несколько конфигураций одного провайдера, используя `alias`. Например, чтобы создать VPC пиринг между двумя регионами AWS в одном проекте."
      }
    ]
  },
  {
    id: 'tj2', title: '2. Переменные и Выводы', desc: 'Variables, Outputs, Locals',
    questions: [
      {
        q: "Variables vs Locals: когда что использовать?",
        a: `<p>Оба используются для хранения значений, но область видимости разная.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Input Variables (var.*):</strong> Это интерфейс вашего модуля. Значения приходят <strong>снаружи</strong> (из <code>terraform.tfvars</code>, командной строки или другого модуля). Используются для параметров, которые должен задавать пользователь.</li>
                <li><strong>Local Values (local.*):</strong> Это внутренние константы или вычисляемые значения. Они видны только <strong>внутри</strong> текущего модуля. Используются для избежания дублирования кода и сложных трансформаций данных.</li>
            </ul>`,
        tip: "Используйте `locals` для формирования сложных имен ресурсов (например, `name = '${var.project}-${var.env}-app'`), чтобы не дублировать эту логику в каждом ресурсе."
      },
      {
        q: "Зачем нужны Outputs?",
        a: `<p><strong>Outputs</strong> выполняют две функции:</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li><strong>Для пользователя (CLI):</strong> Выводят полезную информацию в консоль после <code>apply</code> (например, IP адрес созданного сервера или URL базы данных).</li>
                <li><strong>Для других модулей:</strong> Если вы используете модули, <code>output</code> — это единственный способ вернуть данные из дочернего модуля в родительский. Это как <code>return</code> в функциях программирования.</li>
            </ol>`,
        tip: "Если вы используете `terraform_remote_state`, вы можете читать `outputs` из другого (уже примененного) стека Terraform. Это способ связать разрозненные части инфраструктуры (Networking слой и App слой)."
      }
    ]
  }
];

const MIDDLE_MODULES = [
  {
    id: 'tm1', title: '1. Модули и Структура', desc: 'Modules, DRY, Remote State',
    questions: [
      {
        q: "Remote State: Зачем нужен S3 + DynamoDB (или аналоги)?",
        a: `<p>Хранение <code>terraform.tfstate</code> локально работает только для одного разработчика.</p>
            <p><strong>Проблемы, которые решает Remote State:</strong></p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Shared Access:</strong> Вся команда видит актуальное состояние инфраструктуры.</li>
                <li><strong>Locking (DynamoDB):</strong> Если Вася запустил <code>terraform apply</code>, Петя не сможет запустить его одновременно и сломать стейт. DynamoDB создает запись о блокировке.</li>
                <li><strong>Security:</strong> S3 бакет можно зашифровать и ограничить доступ через IAM, в отличие от файла в Git.</li>
            </ul>`,
        tip: "Всегда включайте версионирование (Versioning) на S3 бакете со стейтом. Если файл стейта повредится, вы сможете откатиться на предыдущую версию в один клик."
      },
      {
        q: "count vs for_each: почему count опасен для списков?",
        a: `<p>Оба используются для создания нескольких копий ресурса.</p>
            <p><strong>Проблема \`count\` со списками:</strong></p>
            <p><code>count</code> опирается на <strong>индекс</strong> (0, 1, 2). Если у вас есть список серверов <code>["web", "app", "db"]</code> и вы удалите "web" (индекс 0), то весь список сместится:</p>
            <ul>
                <li>"app" станет индексом 0 (было 1) -> Terraform пересоздаст сервер!</li>
                <li>"db" станет индексом 1 (было 2) -> Terraform пересоздаст сервер!</li>
            </ul>
            <p><strong>Решение \`for_each\`:</strong></p>
            <p>Опирается на <strong>ключи</strong> карты или строки набора (Set). Удаление элемента не влияет на остальные ресурсы, так как их идентификаторы привязаны к имени, а не к порядковому номеру.</p>`,
        tip: "Используйте `count` только для простых условий (0 или 1) или когда ресурсы абсолютно идентичны и безымянны. Для всего остального — `for_each`."
      },
      {
        q: "Что такое Data Sources и когда их применять?",
        a: `<p><strong>Data Source (data block)</strong> — это запрос на чтение к провайдеру.</p>
            <p>Он позволяет получить информацию о ресурсе, который <strong>не управляется</strong> текущим кодом Terraform (создан вручную, создан другим стеком или существует по умолчанию).</p>
            <p><em>Пример:</em> Найти ID самого свежего AMI образа Ubuntu или получить ID дефолтной VPC.</p>`,
        tip: "Data Sources вычисляются во время `refresh` (перед планом). Если ресурс, который вы ищете, не существует, Terraform упадет с ошибкой еще до начала планирования."
      }
    ]
  },
  {
    id: 'tm2', title: '2. Управление ресурсами', desc: 'Provisioners, Lifecycle, Tainting',
    questions: [
      {
        q: "Почему Provisioners (local-exec, remote-exec) считаются антипаттерном?",
        a: `<p>HashiCorp официально не рекомендует их использование.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Недекларативны:</strong> Terraform не знает, что происходит внутри скрипта. Он не может отследить изменения или удалить то, что скрипт создал.</li>
                <li><strong>Хрупкость:</strong> Зависят от наличия SSH, Python, Bash и сети. Если скрипт упадет посередине, ресурс будет помечен как "Tainted", и Terraform захочет его пересоздать целиком.</li>
                <li><strong>Нет State:</strong> Результат выполнения скрипта не сохраняется в tfstate.</li>
            </ul>
            <p><strong>Альтернатива:</strong> Используйте <code>cloud-init</code> (user_data) для настройки серверов или создавайте "золотые образы" (Packer).</p>`,
        tip: "Единственный легитимный кейс для `local-exec` — это запуск скрипта, который готовит данные для самого Terraform (например, генерация zip-архива для Lambda функции перед деплоем)."
      },
      {
        q: "Lifecycle block: create_before_destroy и ignore_changes",
        a: `<p>Мета-аргумент <code>lifecycle</code> меняет стандартное поведение Terraform.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>create_before_destroy:</strong> По умолчанию Terraform сначала удаляет старый ресурс, потом создает новый (что вызывает даунтайм). Этот флаг инвертирует порядок. (Критично для AutoScaling Groups).</li>
                <li><strong>prevent_destroy:</strong> Защита от случайного удаления (например, базы данных). <code>terraform destroy</code> упадет с ошибкой.</li>
                <li><strong>ignore_changes:</strong> Игнорировать изменения определенных атрибутов. Полезно, если что-то меняется извне (например, количество нод в ASG меняется автоскейлером, и мы не хотим, чтобы Terraform сбрасывал это число обратно при каждом деплое).</li>
            </ul>`,
        tip: "Будьте осторожны с `create_before_destroy`. Некоторые ресурсы (например, EIP или привязка к конкретному порту) не могут существовать в двух экземплярах одновременно, и деплой упадет."
      }
    ]
  }
];

const SENIOR_MODULES = [
  {
    id: 'ts1', title: '1. Рефакторинг и State Manipulation', desc: 'Moved blocks, Import, Terragrunt',
    questions: [
      {
        q: "Как сделать рефакторинг (переименовать ресурс/перенести в модуль) без даунтайма?",
        a: `<p>Раньше для этого использовали команду <code>terraform state mv</code> (императивный подход, опасно и сложно в CI).</p>
            <p>С версии 1.1 появился блок <strong><code>moved</code></strong>:</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
moved {
  from = aws_instance.old_name
  to   = module.web_server.aws_instance.new_name
}
            </div>
            <p>Это декларативный способ сказать Terraform: "Я переименовал ресурс в коде, но в стейте это тот же самый объект, не удаляй его". Terraform автоматически обновит стейт при следующем <code>apply</code>.</p>`,
        tip: "Блоки `moved` можно оставлять в коде навсегда как историю миграций, особенно если вы пишете публичные модули, которыми пользуются другие команды."
      },
      {
        q: "Terraform Import: как взять под контроль существующую инфраструктуру?",
        a: `<p>Если ресурс создан вручную (ClickOps), его нет в tfstate.</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li>Создайте пустой блок ресурса в коде: <code>resource "aws_s3_bucket" "my_bucket" {}</code>.</li>
                <li>Запустите: <code>terraform import aws_s3_bucket.my_bucket bucket-name</code>. Terraform найдет ресурс в облаке и запишет его ID и атрибуты в tfstate.</li>
                <li>Запустите <code>terraform plan</code>. Terraform покажет, что он хочет изменить ресурс, чтобы привести его к (пустому) конфигу.</li>
                <li>Допишите конфигурацию в коде, чтобы <code>plan</code> показал "No changes".</li>
            </ol>
            <p><strong>Terraform 1.5+ (import block):</strong> Появился декларативный блок <code>import</code>, который позволяет генерировать код автоматически (<code>-generate-config-out</code>).</p>`,
        tip: "Импорт — это только запись в State. Terraform НЕ генерирует код для вас (до версии 1.5). Это ручная и кропотливая работа."
      },
      {
        q: "Terragrunt: зачем он нужен, если есть Terraform Modules?",
        a: `<p><strong>Terragrunt</strong> — это обертка над Terraform, решающая проблемы масштабирования (DRY - Don't Repeat Yourself).</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>DRY Backend:</strong> В чистом Terraform нужно копировать конфигурацию \`backend "s3" {...}\` в каждую папку. Terragrunt позволяет определить это один раз в корне.</li>
                <li><strong>DRY Provider:</strong> То же самое для провайдеров.</li>
                <li><strong>Зависимости:</strong> Умеет передавать outputs из одного стека в inputs другого (dependency passing) и запускать их в правильном порядке (apply-all).</li>
                <li><strong>Hooks:</strong> Позволяет запускать скрипты до/после команд.</li>
            </ul>`,
        tip: "Многие фичи Terragrunt (backend config, provider config) постепенно появляются в самом Terraform, но управление зависимостями между разрозненными стеками — все еще киллер-фича Terragrunt."
      }
    ]
  },
  {
    id: 'ts2', title: '2. Продвинутые паттерны', desc: 'Dynamic Blocks, Functions, Validation',
    questions: [
      {
        q: "Dynamic Blocks: как создать вложенные блоки динамически?",
        a: `<p>Используются для ресурсов, которые имеют повторяющиеся вложенные блоки конфигурации (например, <code>ingress</code> правила в <code>aws_security_group</code>).</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
resource "aws_security_group" "example" {
  dynamic "ingress" {
    for_each = var.ingress_rules
    content {
      from_port = ingress.value.port
      to_port   = ingress.value.port
      # ...
    }
  }
}
            </div>
            <p>Это позволяет не копипастить блоки, а генерировать их на основе списка или карты переменных.</p>`,
        tip: "Не злоупотребляйте Dynamic Blocks. Они сильно усложняют чтение кода. Если блоков всего 2-3 и они статичны, лучше написать их явно."
      },
      {
        q: "Custom Validation: как защитить модуль от некорректных данных?",
        a: `<p>В блоке <code>variable</code> можно добавить блок <code>validation</code>.</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
variable "image_id" {
  type = string
  validation {
    condition     = length(var.image_id) > 4 && substr(var.image_id, 0, 4) == "ami-"
    error_message = "The image_id value must be a valid AMI id, starting with 'ami-'."
  }
}
            </div>
            <p>Это позволяет поймать ошибку на этапе валидации/плана, а не ждать падения API провайдера через 10 минут после начала <code>apply</code>.</p>`,
        tip: "Validation condition может использовать только данные из самой переменной. Нельзя валидировать переменную на основе состояния других ресурсов или data sources."
      },
      {
        q: "Drift Detection: что делать, если инфраструктура изменилась вручную?",
        a: `<p>Ситуация: кто-то зашел в консоль AWS и руками изменил Security Group.</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li><strong>Detect:</strong> Запуск <code>terraform plan</code> (или <code>plan --refresh-only</code>) покажет разницу между State и Реальностью.</li>
                <li><strong>Remediate:</strong>
                    <br>- Если ручное изменение было ошибкой: <code>terraform apply</code> вернет все как было в коде.
                    <br>- Если ручное изменение нужно сохранить: необходимо обновить код Terraform, чтобы он соответствовал реальности, и запустить <code>apply</code> (он ничего не изменит, но обновит стейт).</li>
            </ol>`,
        tip: "Настройте периодический запуск `terraform plan` в CI/CD (например, раз в сутки), чтобы получать алерты о дрифте конфигурации (Drift Detection)."
      }
    ]
  }
];

export const course: CourseConfig = {
  id: 'terraform',
  title: 'Terraform IaC',
  description: 'Инфраструктура как код: от `init` до написания сложных модулей и управления State.',
  levels: {
    junior: {
      id: 'junior',
      title: 'Junior',
      subTitle: 'Syntax & Basics',
      icon: '🌱',
      color: 'emerald-500',
      borderColor: 'border-emerald-500',
      bgHover: 'group-hover:bg-emerald-600',
      textHover: 'group-hover:text-emerald-600',
      description: 'Основы HCL, жизненный цикл ресурсов, понимание State файла и работа с провайдерами.',
      modules: JUNIOR_MODULES
    },
    middle: {
      id: 'middle',
      title: 'Middle',
      subTitle: 'Modules & State',
      icon: '⚙️',
      color: 'blue-600',
      borderColor: 'border-blue-600',
      bgHover: 'group-hover:bg-blue-600',
      textHover: 'group-hover:text-blue-600',
      description: 'Написание переиспользуемых модулей, удаленный стейт (Remote State), циклы (for_each) и управление зависимостями.',
      modules: MIDDLE_MODULES
    },
    senior: {
      id: 'senior',
      title: 'Senior',
      subTitle: 'Architect & Scale',
      icon: '🧠',
      color: 'purple-600',
      borderColor: 'border-purple-600',
      bgHover: 'group-hover:bg-purple-600',
      textHover: 'group-hover:text-purple-600',
      description: 'Рефакторинг без даунтайма (moved, import), Terragrunt, CI/CD пайплайны, Drift Detection и Policy as Code.',
      modules: SENIOR_MODULES
    }
  }
};
