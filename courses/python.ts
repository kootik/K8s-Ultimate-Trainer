
import { CourseConfig } from '../types';

const JUNIOR_MODULES = [
  {
    id: 'pj1', title: '1. Основы языка (Core)', desc: 'Types, Structures, Syntax',
    questions: [
      {
        q: "Чем отличаются list и tuple? И что такое hashable?",
        a: `<p><strong>List (Список):</strong> Изменяемый (mutable). Занимает больше памяти (оверхед на динамическое расширение). Не может быть ключом словаря.</p>
            <p><strong>Tuple (Кортеж):</strong> Неизменяемый (immutable). Занимает меньше памяти. Работает быстрее.</p>
            <p><strong>Hashable (Хешируемый):</strong> Объект, хеш которого не меняется в течение жизни. Только хешируемые объекты могут быть ключами <code>dict</code> или элементами <code>set</code>. Кортеж хешируем, только если все его элементы тоже хешируемы.</p>`,
        tip: "Если вам нужен массив констант, всегда используйте Tuple. Это семантически верно и немного быстрее."
      },
      {
        q: "Как работают декораторы? Синтаксический сахар @.",
        a: `<p>Декоратор — это функция, которая принимает другую функцию и возвращает новую функцию, расширяя поведение исходной без изменения её кода.</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
@my_decorator
def func(): pass

# Эквивалентно:
func = my_decorator(func)
            </div>
            <p>Часто используется <code>functools.wraps</code>, чтобы сохранить <code>__name__</code> и <code>__doc__</code> оригинальной функции.</p>`,
        tip: "На интервью могут попросить написать декоратор с аргументами. Помните, что это три уровня вложенности функций."
      },
      {
        q: "Разница между `is` и `==`?",
        a: `<p><strong>== (Equality):</strong> Сравнивает <em>значения</em> объектов. Вызывает метод <code>__eq__</code>.</p>
            <p><strong>is (Identity):</strong> Сравнивает <em>адреса памяти</em> (id). Проверяет, являются ли переменные ссылками на один и тот же объект.</p>
            <p>Пример: <code>[] == []</code> (True), но <code>[] is []</code> (False).</p>`,
        tip: "Для сравнения с `None` всегда используйте `is None`, так как `==` может быть перегружен."
      }
    ]
  },
  {
    id: 'pj2', title: '2. Функции и Области видимости', desc: 'Args, Kwargs, Scopes',
    questions: [
      {
        q: "Что такое *args и **kwargs?",
        a: `<p>Механизм передачи переменного количества аргументов.</p>
            <ul class="list-disc pl-5 space-y-1">
                <li><code>*args</code>: Собирает позиционные аргументы в <strong>кортеж</strong>.</li>
                <li><code>**kwargs</code>: Собирает именованные аргументы в <strong>словарь</strong>.</li>
            </ul>
            <p>Порядок в сигнатуре важен: <code>def func(param, *args, default=None, **kwargs)</code>.</p>`,
        tip: "В Python 3 можно требовать keyword-only аргументы, поставив звездочку: `def f(*, kw_only)`."
      },
      {
        q: "Что такое LEGB rule?",
        a: `<p>Правило поиска имен переменных (Scope Resolution):</p>
            <ol class="list-decimal pl-5 space-y-1">
                <li><strong>L (Local):</strong> Внутри функции.</li>
                <li><strong>E (Enclosing):</strong> Внутри внешней функции (для вложенных).</li>
                <li><strong>G (Global):</strong> На уровне модуля.</li>
                <li><strong>B (Built-in):</strong> Встроенные имена (len, str).</li>
            </ol>`,
        tip: "Если вы хотите изменить глобальную переменную внутри функции, нужно явно указать `global var_name`."
      }
    ]
  }
];

const MIDDLE_MODULES = [
  {
    id: 'pm1', title: '1. Итераторы и Генераторы', desc: 'Yield, Protocol',
    questions: [
      {
        q: "В чем разница между итератором и генератором?",
        a: `<p><strong>Итератор:</strong> Любой объект, реализующий протокол итератора (методы <code>__iter__</code> и <code>__next__</code>).</p>
            <p><strong>Генератор:</strong> Упрощенный способ создания итератора с помощью функции и ключевого слова <code>yield</code>. Генератор автоматически сохраняет состояние функции между вызовами.</p>
            <p><em>Преимущество:</em> Lazy evaluation (ленивое вычисление). Не загружает все данные в память сразу, выдавая их по одному.</p>`,
        tip: "Генераторные выражения `(x for x in data)` — отличный способ сэкономить память вместо List Comprehensions `[x for x in data]`."
      },
      {
        q: "Как работает Context Manager (with)?",
        a: `<p>Позволяет безопасно управлять ресурсами (файлы, соединения с БД).</p>
            <p>Класс должен реализовать методы:</p>
            <ul class="list-disc pl-5 space-y-1">
                <li><code>__enter__</code>: Инициализация, возврат ресурса.</li>
                <li><code>__exit__</code>: Очистка, обработка исключений. Вызывается всегда, даже при ошибке.</li>
            </ul>
            <p>Также можно использовать декоратор <code>@contextlib.contextmanager</code> с <code>yield</code>.</p>`,
        tip: "Это основа обработки транзакций в БД. `__exit__` возвращает `True`, чтобы подавить всплытие исключения."
      }
    ]
  },
  {
    id: 'pm2', title: '2. Асинхронность (Asyncio)', desc: 'Event Loop, Coroutines',
    questions: [
      {
        q: "Как работает Event Loop в Python?",
        a: `<p>Python (asyncio) использует <strong>кооперативную многозадачность</strong> в одном потоке.</p>
            <ul class="list-disc pl-5 space-y-1">
                <li><strong>Event Loop:</strong> Бесконечный цикл, который мониторит задачи.</li>
                <li><strong>await:</strong> Передает управление обратно в Event Loop, говоря "я жду I/O, выполняй пока другие задачи".</li>
                <li><strong>Non-blocking I/O:</strong> Пока одна корутина ждет ответа от сети, поток не блокируется, а выполняет код другой корутины.</li>
            </ul>`,
        tip: "Asyncio не ускоряет CPU-bound задачи. Для них нужны процессы (multiprocessing). Asyncio идеален для I/O-bound (сеть, диск)."
      },
      {
        q: "Multithreading vs Multiprocessing в Python?",
        a: `<p><strong>Multithreading (Потоки):</strong> Работают в одном процессе, делят память. Из-за <strong>GIL</strong> (Global Interpreter Lock) только один поток может выполнять байт-код Python в один момент времени. Полезно только для I/O.</p>
            <p><strong>Multiprocessing (Процессы):</strong> Каждый процесс имеет свой интерпретатор и память. Обходят GIL. Используют все ядра CPU. Накладные расходы на создание и IPC (Inter-Process Communication).</p>`,
        tip: "В 99% веб-бэкендов мы используем комбинацию: Gunicorn (N процессов) + Uvicorn (Asyncio внутри процессов)."
      }
    ]
  }
];

const SENIOR_MODULES = [
  {
    id: 'ps1', title: '1. Python Internals', desc: 'Memory, GIL, CPython',
    questions: [
      {
        q: "Как работает управление памятью и Garbage Collection?",
        a: `<p>Основной механизм: <strong>Reference Counting (Подсчет ссылок)</strong>. Как только счетчик ссылок на объект падает до 0, память освобождается мгновенно.</p>
            <p>Дополнительный механизм: <strong>Generational GC</strong> (Сборщик мусора поколений). Нужен для обнаружения <strong>циклических ссылок</strong> (A ссылается на B, B на A). GC периодически сканирует объекты, разделенные на 3 поколения (молодые, средние, старые).</p>`,
        tip: "Вызов `gc.collect()` вручную в проде может вызвать 'Stop-the-world' фризы. Лучше тюнить пороги срабатывания."
      },
      {
        q: "Что такое метаклассы (Metaclasses)?",
        a: `<p>Метакласс — это "класс класса". Он управляет процессом создания самого класса (как объекта).</p>
            <p>По умолчанию в Python метакласс — <code>type</code>.</p>
            <p>Используются для:</p>
            <ul class="list-disc pl-5 space-y-1">
                <li>Валидации атрибутов класса при создании.</li>
                <li>Автоматической регистрации плагинов/моделей (как в Django ORM).</li>
                <li>Изменения MRO или добавления методов "на лету".</li>
            </ul>`,
        tip: "Правило Тима Питерса: 'Если вы сомневаетесь, нужны ли вам метаклассы — они вам не нужны'."
      },
      {
        q: "Как работает MRO (Method Resolution Order)?",
        a: `<p>Порядок, в котором Python ищет методы в иерархии наследования.</p>
            <p>Используется алгоритм <strong>C3 Linearization</strong>.</p>
            <p>Основные принципы:</p>
            <ul class="list-disc pl-5 space-y-1">
                <li>Дети идут раньше родителей.</li>
                <li>Если класс наследуется от нескольких родителей, порядок родителей сохраняется.</li>
                <li>Монотонность (порядок не меняется при наследовании).</li>
            </ul>
            <p>Посмотреть MRO можно через <code>ClassName.mro()</code>.</p>`,
        tip: "Проблема 'Ромбовидного наследования' решается именно через MRO и вызовы `super()`."
      }
    ]
  },
  {
    id: 'ps2', title: '2. Architecture & Scale', desc: 'Django, Scaling, Optimization',
    questions: [
      {
        q: "N+1 проблема в ORM: как обнаружить и лечить?",
        a: `<p>Проблема, когда для получения списка объектов делается 1 запрос, а потом для каждого связанного объекта делается еще по 1 запросу (N раз).</p>
            <p><strong>Лечение (Django):</strong></p>
            <ul class="list-disc pl-5 space-y-1">
                <li><code>select_related</code>: Использует SQL JOIN. Для связей 1-to-1 и ForeignKey.</li>
                <li><code>prefetch_related</code>: Делает отдельный запрос с \`WHERE id IN (...)\` и склеивает данные на уровне Python. Для Many-to-Many.</li>
            </ul>`,
        tip: "Используйте `django-debug-toolbar` или `silk` для профилирования SQL запросов в разработке."
      },
      {
        q: "Celery: At-least-once vs Exactly-once delivery",
        a: `<p>Celery (и большинство систем очередей) гарантируют <strong>At-least-once</strong> (хотя бы один раз).</p>
            <p>Это значит, что задача может выполниться дважды (например, если воркер упал после выполнения, но до отправки ACK брокеру).</p>
            <p><strong>Решение:</strong> Задачи должны быть <strong>идемпотентными</strong>. Повторное выполнение задачи не должно ломать систему (проверять статус перед выполнением, использовать уникальные ключи идемпотентности).</p>`,
        tip: "Никогда не передавайте сложные объекты (ORM модели) в аргументы задачи Celery. Передавайте только ID."
      }
    ]
  }
];

export const course: CourseConfig = {
  id: 'python',
  title: 'Python Backend',
  description: 'Путь от скриптов до высоконагруженных систем: Asyncio, Django/FastAPI, Internals.',
  levels: {
    junior: {
      id: 'junior',
      title: 'Junior',
      subTitle: 'Синтаксис и База',
      icon: '🐍',
      color: 'emerald-500',
      borderColor: 'border-emerald-500',
      bgHover: 'group-hover:bg-emerald-600',
      textHover: 'group-hover:text-emerald-600',
      description: 'Основы языка Python, структуры данных, работа с функциями и базовое понимание ООП.',
      modules: JUNIOR_MODULES
    },
    middle: {
      id: 'middle',
      title: 'Middle',
      subTitle: 'Async & Tools',
      icon: '🛠️',
      color: 'blue-600',
      borderColor: 'border-blue-600',
      bgHover: 'group-hover:bg-blue-600',
      textHover: 'group-hover:text-blue-600',
      description: 'Продвинутые темы: асинхронное программирование, итераторы, декораторы и работа с базами данных.',
      modules: MIDDLE_MODULES
    },
    senior: {
      id: 'senior',
      title: 'Senior',
      subTitle: 'CPython & Arch',
      icon: '🏗️',
      color: 'purple-600',
      borderColor: 'border-purple-600',
      bgHover: 'group-hover:bg-purple-600',
      textHover: 'group-hover:text-purple-600',
      description: 'Глубокое погружение: как работает интерпретатор (GIL, GC), метапрограммирование и архитектура масштабируемых приложений.',
      modules: SENIOR_MODULES
    }
  }
};
