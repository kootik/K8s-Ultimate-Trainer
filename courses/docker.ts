

import { CourseConfig } from '../types';

const JUNIOR_MODULES = [
  {
    id: 'dj1', title: '1. Основы Контейнеризации', desc: 'Images, Containers, Dockerfile',
    questions: [
      {
        q: "В чем разница между Образом (Image) и Контейнером (Container)?",
        a: `<p><strong>Image (Образ):</strong> Это неизменяемый (read-only) шаблон, содержащий код приложения, библиотеки и зависимости. Это как "класс" в ООП или файл <code>.exe</code> на диске.</p>
            <p><strong>Container (Контейнер):</strong> Это запущенный экземпляр образа. У него появляется свой слой записи (Read-Write Layer) поверх слоев образа. Это как "объект" в ООП или запущенный процесс в памяти.</p>
            <p>Из одного образа можно запустить сотни контейнеров.</p>`,
        tip: "Хорошая аналогия: Образ — это рецепт торта, а Контейнер — это испеченный торт. Вы можете испечь много тортов по одному рецепту и украсить (изменить) каждый по-разному."
      },
      {
        q: "CMD vs ENTRYPOINT: В чем подвох?",
        a: `<p>Обе инструкции определяют, что запустится внутри контейнера, но работают по-разному при передаче аргументов.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>CMD:</strong> Задает команду по умолчанию. Легко переопределяется при запуске: <code>docker run my-image bash</code> заменит CMD.</li>
                <li><strong>ENTRYPOINT:</strong> Задает исполняемый файл, который <em>всегда</em> будет запущен. Аргументы из <code>docker run</code> добавляются к нему как параметры.</li>
            </ul>
            <p><strong>Best Practice:</strong> Использовать <code>ENTRYPOINT ["/bin/app"]</code> для бинарника и <code>CMD ["--help"]</code> для аргументов по умолчанию.</p>`,
        tip: "Помните про Shell form и Exec form. Всегда используйте Exec form (квадратные скобки `[\"cmd\"]`), иначе PID 1 будет `/bin/sh`, который не передает сигналы (SIGTERM) вашему приложению."
      },
      {
        q: "Зачем нужен .dockerignore и почему это важно?",
        a: `<p>Этот файл работает как <code>.gitignore</code>. Он исключает файлы из контекста сборки (Build Context), который отправляется демону Docker.</p>
            <h4 class="font-bold mt-2">Зачем нужен:</h4>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li><strong>Скорость:</strong> Не отправлять гигабайты <code>node_modules</code> или <code>.git</code> папки демону перед началом билда.</li>
                <li><strong>Безопасность:</strong> Предотвратить случайное попадание <code>.env</code> файлов, ключей и паролей в слои образа.</li>
                <li><strong>Кэширование:</strong> Изменение игнорируемых файлов не будет инвалидировать кэш сборки.</li>
            </ol>`,
        tip: "Если билд начинается с долгой паузы 'Sending build context to Docker daemon' — вы забыли настроить .dockerignore."
      }
    ]
  },
  {
    id: 'dj2', title: '2. Работа с данными и слоями', desc: 'Layers, Volumes',
    questions: [
      {
        q: "Как работает слоеная файловая система (UnionFS/OverlayFS)?",
        a: `<p>Docker образ состоит из слоев (Layers). Каждая инструкция в Dockerfile (RUN, COPY, ADD) создает новый слой.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Read-Only:</strong> Все слои образа доступны только для чтения.</li>
                <li><strong>Copy-on-Write (CoW):</strong> Когда контейнер хочет изменить файл из нижнего слоя, Docker копирует этот файл в верхний (RW) слой контейнера и меняет его там. Оригинал остается нетронутым.</li>
            </ul>
            <p>Это экономит место: 10 контейнеров Ubuntu занимают место как 1 Ubuntu + дельты изменений.</p>`,
        tip: "Именно поэтому важно удалять кэши (apt-get clean) в ТОЙ ЖЕ строке RUN. Если удалить в следующем RUN, файл исчезнет из вида, но останется физически в предыдущем слое."
      },
      {
        q: "Volume vs Bind Mount: когда что использовать?",
        a: `<p>Оба механизма пробрасывают данные внутрь контейнера.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Bind Mount:</strong> Пробрасывает конкретную папку с хоста (<code>-v /home/user/code:/app</code>).
                    <br><em>Use Case:</em> Локальная разработка, конфиги. Зависит от файловой структуры хоста.</li>
                <li><strong>Volume (Named Volume):</strong> Хранилище, управляемое Docker (в <code>/var/lib/docker/volumes/</code>).
                    <br><em>Use Case:</em> Базы данных, персистентные данные в проде. Более безопасно, переносимо, работает с драйверами (NFS, Cloud).</li>
            </ul>`,
        tip: "При использовании Docker Desktop на Mac/Windows, Named Volumes работают значительно быстрее, чем Bind Mounts из-за особенностей виртуализации файловой системы."
      }
    ]
  }
];

const MIDDLE_MODULES = [
  {
    id: 'dm1', title: '1. Сетевое взаимодействие', desc: 'Bridge, Host, DNS',
    questions: [
      {
        q: "Bridge vs Host Network: влияние на производительность и изоляцию",
        a: `<p>Два основных драйвера сети:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Bridge (Default):</strong> Контейнер получает свой IP в виртуальной подсети. Docker использует NAT и iptables для проброса портов (<code>-p 80:80</code>).
                    <br><em>Минус:</em> Оверхед на NAT.</li>
                <li><strong>Host:</strong> Контейнер делит сетевой стек с хостом. Нет изоляции сети.
                    <br><em>Плюс:</em> Максимальная производительность (нет NAT).
                    <br><em>Минус:</em> Конфликты портов (нельзя запустить два Nginx на 80 порту). Не работает на Docker Desktop (Mac/Win) из-за VM.</li>
            </ul>`,
        tip: "В продакшене для высоконагруженных систем (High Load) часто используют `network_mode: host`, чтобы избежать накладных расходов conntrack/NAT."
      },
      {
        q: "Как контейнеры находят друг друга? (Docker DNS)",
        a: `<p>Docker имеет встроенный DNS сервер по адресу <code>127.0.0.11</code>.</p>
            <p>Если контейнеры находятся в <strong>пользовательской сети</strong> (User-defined bridge network), они могут обращаться друг к другу по <strong>имени контейнера</strong> (или имени сервиса в Compose).</p>
            <p>В дефолтной сети <code>bridge</code> DNS по имени не работает (работает только по IP или через --link, который deprecated). Это сделано для обратной совместимости.</p>`,
        tip: "Всегда создавайте свою сеть `docker network create my-net` или используйте docker-compose, который делает это автоматически."
      }
    ]
  },
  {
    id: 'dm2', title: '2. Оптимизация сборки', desc: 'Multi-stage, Caching',
    questions: [
      {
        q: "Объясни концепцию Multi-stage builds. Зачем это нужно?",
        a: `<p>Позволяет использовать один Dockerfile для сборки и для финального образа, отбрасывая лишнее.</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
FROM golang:1.21 AS builder
RUN go build -o app main.go

FROM alpine:latest
COPY --from=builder /app .
CMD ["./app"]
            </div>
            <p><strong>Результат:</strong> В финальном образе нет компилятора Go, исходников и кэшей сборки. Образ весит 10MB вместо 800MB.</p>`,
        tip: "Это также улучшает безопасность: в проде нет инструментов сборки, которыми мог бы воспользоваться злоумышленник."
      },
      {
        q: "Порядок команд в Dockerfile влияет на кэш. Как оптимизировать?",
        a: `<p>Docker кэширует слои. Если слой изменился, все последующие слои пересобираются заново.</p>
            <p><strong>Правило:</strong> Сначала копируйте то, что меняется редко (зависимости), потом то, что меняется часто (код).</p>
            <p><em>Плохо:</em><br><code>COPY . .</code><br><code>RUN npm install</code> (Любое изменение в коде инвалидирует кэш npm install)</p>
            <p><em>Хорошо:</em><br><code>COPY package.json .</code><br><code>RUN npm install</code><br><code>COPY . .</code></p>`,
        tip: "Самые 'нестабильные' инструкции должны быть в самом низу Dockerfile."
      }
    ]
  }
];

const SENIOR_MODULES = [
  {
    id: 'ds1', title: '1. Docker Internals (Under the hood)', desc: 'Namespaces, Cgroups, Runc',
    questions: [
      {
        q: "Что такое контейнер на уровне ядра Linux? (Namespaces & Cgroups)",
        a: `<p>Контейнеров в ядре Linux не существует. Это абстракция userspace.</p>
            <p>Она строится на двух китах:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Namespaces (Изоляция - Что я вижу?):</strong>
                    <br>- PID: Свои процессы (PID 1).
                    <br>- NET: Свои интерфейсы и порты.
                    <br>- MNT: Своя файловая система.
                    <br>- USER, IPC, UTS.</li>
                <li><strong>Cgroups (Control Groups - Ограничения - Сколько я могу взять?):</strong>
                    <br>- Лимитируют потребление CPU, Memory, Disk I/O.
                    <br>- Позволяют убить (OOM Kill) группу процессов при превышении лимита.</li>
            </ul>`,
        tip: "Попробуйте команду `unshare --fork --pid --mount-proc bash`. Вы создадите 'контейнер' вручную, используя только системные вызовы Linux без Docker."
      },
      {
        q: "Проблема PID 1 (Zombie Processes) в контейнерах. Как решать?",
        a: `<p>В Linux процесс с PID 1 (init system, systemd) имеет особую роль: он обязан "усыновлять" сирот и "жинать" (reap) зомби-процессы (завершенные процессы, ожидающие чтения кода возврата).</p>
            <p>Если ваше приложение (Node.js, Java) запущено как PID 1, оно часто не умеет обрабатывать сигналы зомби. Таблица процессов переполняется, и контейнер падает.</p>
            <p><strong>Решение:</strong></p>
            <ul class="list-disc pl-5 space-y-1">
                <li>Использовать <code>tini</code> (флаг <code>--init</code> в docker run).</li>
                <li>Использовать bash скрипт как entrypoint (bash умеет reap zombies).</li>
                <li>Настроить приложение правильно.</li>
            </ul>`,
        tip: "Если `docker stop` висит 10 секунд перед убийством контейнера — это значит, PID 1 не обрабатывает SIGTERM и Docker вынужден слать SIGKILL. Это тоже проблема PID 1."
      },
      {
        q: "Docker Socket Mounting (/var/run/docker.sock). В чем риск?",
        a: `<p>Проброс сокета демона внутрь контейнера: <code>-v /var/run/docker.sock:/var/run/docker.sock</code>.</p>
            <p>Это дает контейнеру <strong>полный root доступ к хосту</strong>. Контейнер может:</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li>Запустить новый контейнер с <code>privileged</code> флагом.</li>
                <li>Примонтировать корень хоста <code>/</code> к этому контейнеру.</li>
                <li>Удалить или изменить любые файлы на хосте.</li>
            </ol>
            <p>Это эквивалентно выдаче <code>sudo</code> без пароля.</p>`,
        tip: "Для CI/CD (Docker-in-Docker) безопаснее использовать Sysbox или Kaniko, которые не требуют проброса сокета хоста."
      },
      {
        q: "Что такое OCI (Open Container Initiative) и зачем нужны стандарты?",
        a: `<p><strong>OCI</strong> — это организация, создавшая открытые стандарты для контейнеров, чтобы прекратить войны форматов.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Image Spec:</strong> Стандарт формата образа (слои, JSON манифест, конфиг). Гарантирует, что образ, собранный в Docker, запустится в Podman или Kubernetes.</li>
                <li><strong>Runtime Spec:</strong> Стандарт запуска контейнера (lifecycle, создание namespaces/cgroups). Референсная реализация — <strong>runc</strong>.</li>
            </ul>
            <p>Благодаря OCI, Kubernetes смог отказаться от Docker (Dockershim) и переключиться на containerd/CRI-O, так как все они работают с одними и теми же OCI-совместимыми образами.</p>`,
        tip: "Знание OCI отличает инженера, понимающего экосистему, от того, кто знает только команды `docker run`."
      }
    ]
  },
  {
    id: 'ds2', title: '2. Безопасность и OCI', desc: 'Rootless, Seccomp, Distroless',
    questions: [
      {
        q: "Rootless Docker vs Root Docker. Зачем страдать?",
        a: `<p>По умолчанию Docker Daemon работает от <strong>root</strong>. Если злоумышленник вырвется из контейнера (Container Escape vulnerability), он станет root на хосте.</p>
            <p><strong>Rootless mode:</strong> Запускает демон и контейнеры от имени обычного пользователя, используя User Namespaces для маппинга UID (внутри контейнера вы root, снаружи — user 1000).</p>
            <p><em>Ограничения:</em> Нельзя биндить порты < 1024, сложности с Overlay сетью.</p>`,
        tip: "Podman изначально создавался как rootless-решение и не имеет центрального демона. Это часто делает его более безопасным выбором для HPC систем."
      },
      {
        q: "Что такое Distroless образы?",
        a: `<p>Это образы (обычно от Google), в которых есть <strong>только</strong> ваше приложение и его runtime-зависимости. Там нет:</p>
            <ul class="list-disc pl-5 space-y-1">
                <li>Пакетного менеджера (apt, yum, apk).</li>
                <li>Shell (bash, sh).</li>
                <li>Утилит (ls, curl, cat).</li>
            </ul>
            <p><strong>Зачем:</strong> Если хакер взломает приложение (RCE), он не сможет запустить шелл или скачать майнер, так как <code>sh</code> и <code>curl</code> просто отсутствуют.</p>`,
        tip: "Дебажить такие контейнеры сложно (нет sh/ls). Используйте `kubectl debug -it <pod> --image=busybox:1.28 --target=<container>`. Это подключит 'Ephemeral Container' с набором утилит к процессу вашего distroless-контейнера."
      }
    ]
  }
];

export const course: CourseConfig = {
  id: 'docker',
  title: 'Docker Expert',
  description: 'От Dockerfile до ядра Linux: Cgroups, Namespaces и безопасность контейнеров.',
  levels: {
    junior: {
      id: 'junior',
      title: 'Junior',
      subTitle: 'Концепции',
      icon: '🐳',
      color: 'sky-500',
      borderColor: 'border-sky-500',
      bgHover: 'group-hover:bg-sky-600',
      textHover: 'group-hover:text-sky-600',
      description: 'Базовое понимание: образы, контейнеры, написание Dockerfile, работа с docker-compose и основными командами CLI.',
      modules: JUNIOR_MODULES
    },
    middle: {
      id: 'middle',
      title: 'Middle',
      subTitle: 'Optimization',
      icon: '📦',
      color: 'indigo-500',
      borderColor: 'border-indigo-500',
      bgHover: 'group-hover:bg-indigo-600',
      textHover: 'group-hover:text-indigo-600',
      description: 'Оптимизация сборки (Multi-stage), работа с сетью, управление данными (Volumes) и оркестрация.',
      modules: MIDDLE_MODULES
    },
    senior: {
      id: 'senior',
      title: 'Senior',
      subTitle: 'Kernel & Security',
      icon: '⚓',
      color: 'rose-600',
      borderColor: 'border-rose-600',
      bgHover: 'group-hover:bg-rose-600',
      textHover: 'group-hover:text-rose-600',
      description: 'Внутренности (Namespaces, Cgroups, UnionFS), безопасность (Seccomp, Capabilities, Rootless) и стандарты OCI.',
      modules: SENIOR_MODULES
    }
  }
};
