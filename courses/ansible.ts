
import { CourseConfig } from '../types';

const JUNIOR_MODULES = [
  {
    id: 'aj1', title: '1. Основы Ansible', desc: 'Inventory, Playbooks, Idempotency',
    questions: [
      {
        q: "Что такое идемпотентность и почему она критична для Ansible?",
        a: `<p><strong>Идемпотентность</strong> — это свойство операции, при котором повторное выполнение действия не меняет состояние системы, если она уже находится в целевом состоянии.</p>
            <p>В контексте Ansible:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li>Если вы запускаете плейбук 10 раз подряд, изменения (Changes) должны произойти только в первый раз.</li>
                <li>В последующие 9 раз Ansible должен сообщить <code>ok</code> (зеленый цвет), а не <code>changed</code> (желтый).</li>
            </ul>
            <p>Это позволяет безопасно запускать автоматизацию на продакшене, гарантируя предсказуемое конечное состояние (Desired State).</p>`,
        tip: "Модули `shell` и `command` НЕ идемпотентны по умолчанию. Вы должны сами сделать их таковыми, используя аргументы `creates` или `removes`."
      },
      {
        q: "В чем разница между модулями `command` и `shell`?",
        a: `<p>Оба модуля выполняют команды в консоли, но есть нюансы:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>command (по умолчанию):</strong> Выполняет команду напрямую, минуя оболочку (shell). Переменные окружения оболочки (например, <code>$HOME</code>) и операторы (<code>|</code>, <code>></code>, <code>&</code>) <strong>не работают</strong>. Это безопаснее и быстрее.</li>
                <li><strong>shell:</strong> Запускает команду через <code>/bin/sh</code>. Поддерживает пайпы, редиректы и переменные окружения.</li>
            </ul>`,
        tip: "Всегда используйте `command`, если вам не нужны пайпы или редиректы. Использование `shell` может открыть уязвимости (Shell Injection), если переменные не экранированы."
      },
      {
        q: "Как работают Handlers и когда они запускаются?",
        a: `<p><strong>Handlers (Обработчики)</strong> — это специальные задачи, которые запускаются только при получении уведомления (notify).</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li>Задача вызывает handler через ключевое слово <code>notify: Restart Nginx</code>.</li>
                <li>Уведомление срабатывает <strong>только если</strong> задача вернула статус <code>changed</code>.</li>
                <li>Handlers запускаются <strong>в самом конце</strong> выполнения плей (play), а не сразу после задачи.</li>
                <li>Если handler был уведомлен несколько раз разными задачами, он запустится только <strong>один раз</strong>.</li>
            </ul>`,
        tip: "Если плейбук упадет с ошибкой до конца выполнения, Handlers не запустятся. Чтобы принудительно запустить их при ошибке, используйте `--force-handlers` в командной строке."
      }
    ]
  },
  {
    id: 'aj2', title: '2. Инвентарь и Переменные', desc: 'Static vs Dynamic Inventory, Vars',
    questions: [
      {
        q: "Что такое Dynamic Inventory и зачем он нужен?",
        a: `<p>Статический инвентарь (файл <code>hosts.ini</code> или <code>hosts.yaml</code>) требует ручного обновления IP-адресов.</p>
            <p><strong>Dynamic Inventory</strong> — это скрипт (или плагин), который опрашивает внешнюю систему (AWS, Google Cloud, VMware, LDAP) и генерирует JSON со списком хостов и групп на лету.</p>
            <p><em>Пример:</em> В облаке машины создаются автоскейлингом. Dynamic Inventory (плагин <code>aws_ec2</code>) позволяет Ansible узнать актуальные IP-адреса всех инстансов с тегом <code>role: backend</code> прямо перед запуском плейбука.</p>`,
        tip: "В современном Ansible предпочтительнее использовать Inventory Plugins (настраиваются через YAML), а не старые Inventory Scripts (исполняемые файлы)."
      },
      {
        q: "Каков приоритет переменных (Variable Precedence) в Ansible?",
        a: `<p>Ansible имеет сложную систему приоритетов (более 20 уровней). Основное правило: "Чем ближе к моменту выполнения, тем выше приоритет".</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li><strong>Extra vars (<code>-e</code>):</strong> Самый высокий приоритет. Перезаписывают всё.</li>
                <li><strong>Play vars:</strong> Переменные, определенные в блоке <code>vars</code> плейбука.</li>
                <li><strong>Host facts:</strong> Факты, собранные с хоста.</li>
                <li><strong>Inventory vars (Host/Group):</strong> Переменные из файлов инвентаря.</li>
                <li><strong>Role defaults:</strong> Самый низкий приоритет.</li>
            </ol>`,
        tip: "Старайтесь не размазывать переменные по 10 местам. Лучшая практика — хранить почти все в `group_vars` и `host_vars`. Используйте `-e` только для отладки или CI/CD пайплайнов."
      }
    ]
  }
];

const MIDDLE_MODULES = [
  {
    id: 'am1', title: '1. Структурирование кода', desc: 'Roles, Includes vs Imports',
    questions: [
      {
        q: "Anatomy of a Role: опиши структуру и назначение директорий.",
        a: `<p>Роль — это стандартный способ упаковки задач, переменных и файлов для переиспользования.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><code>tasks/main.yml</code>: Основной список действий.</li>
                <li><code>handlers/main.yml</code>: Обработчики (рестарты сервисов).</li>
                <li><code>defaults/main.yml</code>: Переменные с низким приоритетом (легко переопределить).</li>
                <li><code>vars/main.yml</code>: Переменные с высоким приоритетом (трудно переопределить).</li>
                <li><code>files/</code>: Статичные файлы для копирования (модуль <code>copy</code>).</li>
                <li><code>templates/</code>: Jinja2 шаблоны (модуль <code>template</code>).</li>
                <li><code>meta/main.yml</code>: Зависимости роли и метаданные (автор, лицензия).</li>
            </ul>`,
        tip: "Никогда не храните секреты в `vars/`. Используйте `defaults/` для пустых значений или Ansible Vault. Переменные в `vars/` роли перекрывают даже переменные инвентаря!"
      },
      {
        q: "В чем фундаментальная разница между `import_*` и `include_*`?",
        a: `<p>Это разница между <strong>статическим</strong> и <strong>динамическим</strong> включением.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Import (Static):</strong> Файлы считываются и вставляются в плейбук <strong>в момент парсинга</strong> (до начала выполнения).
                    <br>- Нельзя использовать в циклах.
                    <br>- Переменные из файла видны везде.</li>
                <li><strong>Include (Dynamic):</strong> Файл обрабатывается <strong>в момент выполнения</strong> задачи.
                    <br>- Можно использовать в циклах (<code>loop</code>).
                    <br>- Имя файла может содержать переменную.</li>
            </ul>`,
        tip: "Если вы используете `notify` на задачу внутри include-файла, Ansible может не увидеть хендлер из-за динамической природы. С `import` таких проблем нет."
      }
    ]
  },
  {
    id: 'am2', title: '2. Логика и управление потоком', desc: 'Blocks, Filters, Error Handling',
    questions: [
      {
        q: "Как обработать ошибки в плейбуке, не прерывая выполнение (try-catch)?",
        a: `<p>Используйте конструкцию <code>block</code>, <code>rescue</code> и <code>always</code>.</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
- block:
    - name: Опасная операция
      command: /bin/false
  rescue:
    - name: Откат изменений
      command: /bin/rollback
  always:
    - name: Этот шаг выполнится всегда
      debug: msg="Cleanup..."
            </div>
            <p>Это аналог <code>try/except/finally</code> в Python.</p>`,
        tip: "Также полезен параметр `ignore_errors: true` для конкретной задачи, если её провал не критичен."
      },
      {
        q: "Зачем нужны `changed_when` и `failed_when`?",
        a: `<p>Они позволяют переопределить стандартную логику возврата статуса модулем.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>changed_when:</strong> Управляет тем, когда задача считается изменившей состояние.
                    <br><em>Пример:</em> <code>command: ls</code> всегда возвращает changed. Добавьте <code>changed_when: false</code>, чтобы Ansible считал это "ok" (зеленым) и не триггерил хендлеры.</li>
                <li><strong>failed_when:</strong> Определяет, что считать ошибкой.
                    <br><em>Пример:</em> Команда возвращает exit code 1, но для нас это норма. Пишем: <code>failed_when: "'Error' in result.stderr"</code>.</li>
            </ul>`,
        tip: "Использование `shell/command` без `changed_when` — признак плохого тона. Это ломает отчетность идемпотентности."
      }
    ]
  },
  {
    id: 'am3', title: '3. Jinja2 и манипуляции данными', desc: 'Filters, Loops, Lookups',
    questions: [
      {
        q: "Как работает `loop` (или `with_items`) и как фильтровать списки?",
        a: `<p><code>loop</code> итерируется по списку. Для сложной обработки данных используются <strong>Jinja2 фильтры</strong>.</p>
            <p>Примеры фильтров:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><code>map</code>: Извлечь атрибут из списка объектов.</li>
                <li><code>selectattr</code> / <code>rejectattr</code>: Фильтрация списка по атрибутам.</li>
                <li><code>json_query</code>: Мощный поиск по JSON (использует JMESPath).</li>
            </ul>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
loop: "{{ users | selectattr('state', 'equalto', 'active') | map(attribute='name') }}"
            </div>`,
        tip: "`with_items` автоматически 'сплющивает' (flatten) вложенные списки, а `loop` — нет. В новых версиях Ansible рекомендуется использовать `loop`."
      },
      {
        q: "Host vars vs Group vars: где что хранить?",
        a: `<p><strong>Group vars:</strong> Переменные, общие для группы серверов (например, <code>db_servers</code>). Хранятся в <code>group_vars/db_servers.yml</code>.
            <br><em>Пример:</em> Версия PostgreSQL, путь к логам.</p>
            <p><strong>Host vars:</strong> Специфичные для конкретного хоста. Хранятся в <code>host_vars/hostname.yml</code>.
            <br><em>Пример:</em> <code>ansible_host</code> (IP адрес), уникальный ID сервера (server_id).</p>`,
        tip: "Не забывайте про специальную группу `all`. Файл `group_vars/all.yml` — отличное место для глобальных дефолтных значений."
      }
    ]
  }
];

const SENIOR_MODULES = [
  {
    id: 'as1', title: '1. Производительность и Масштабирование', desc: 'Forks, Strategy, Pipelining',
    questions: [
      {
        q: "Что такое Ansible Pipelining и как он ускоряет выполнение?",
        a: `<p>По умолчанию Ansible для выполнения одной задачи делает несколько SSH-подключений:</p>
            <ol class="list-decimal pl-5 space-y-1">
                <li>Создать временную директорию.</li>
                <li>Загрузить модуль (SFTP/SCP).</li>
                <li>Сделать файл исполняемым.</li>
                <li>Запустить модуль.</li>
                <li>Удалить директорию.</li>
            </ol>
            <p><strong>Pipelining = True</strong> (в <code>ansible.cfg</code>) сокращает это число, передавая модуль напрямую в STDIN питона через одно SSH-соединение. Это ускоряет работу в разы.</p>`,
        tip: "Pipelining требует отключенного `requiretty` в настройках `sudoers` на целевых машинах, иначе sudo не сработает."
      },
      {
        q: "Strategy: linear vs free. Как ускорить деплой на 1000 серверов?",
        a: `<p>Ansible выполняет задачи синхронно для батча хостов.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>linear (default):</strong> Все хосты ждут, пока <em>каждый</em> закончит задачу 1, прежде чем начать задачу 2. Если один хост тормозит, ждут все.</li>
                <li><strong>free:</strong> Каждый хост бежит по плейбуку так быстро, как может, не дожидаясь других. Порядок задач сохраняется внутри хоста, но рассинхронизируется между хостами.</li>
            </ul>
            <p>Также важен параметр <strong>forks</strong> (по умолчанию 5). Увеличьте его до 50-100 при наличии ресурсов CPU.</p>`,
        tip: "Используйте `strategy: free` для операций типа 'patching' или 'gathering facts', где нет зависимостей между узлами. Для оркестрации (app -> db) нужен `linear`."
      },
      {
        q: "Mitogen for Ansible: что это и стоит ли использовать?",
        a: `<p><strong>Mitogen</strong> — это сторонний плагин стратегии, который полностью заменяет сетевой слой Ansible.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li>Вместо постоянных SSH reconnects и пересылки Python-кода, он держит туннель открытым и кэширует код на удаленных машинах.</li>
                <li>Уменьшает использование CPU на управляющей машине и трафик.</li>
                <li>Ускорение выполнения плейбуков: от 1.5x до 7x.</li>
            </ul>`,
        tip: "Mitogen может ломаться при мажорных обновлениях Ansible. Используйте с осторожностью и тестируйте. В Ansible 2.10+ нативные улучшения производительности сократили разрыв, но Mitogen все еще быстрее."
      }
    ]
  },
  {
    id: 'as2', title: '2. Разработка модулей и плагинов', desc: 'Python API, Custom Modules',
    questions: [
      {
        q: "Как написать свой модуль для Ansible? (Custom Module)",
        a: `<p>Модули пишутся на Python (или любом языке, который может вернуть JSON, но Python нативен).</p>
            <p>Основные шаги:</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li>Импортировать <code>AnsibleModule</code> из <code>ansible.module_utils.basic</code>.</li>
                <li>Определить <code>argument_spec</code> (входные параметры и их типы).</li>
                <li>Выполнить логику.</li>
                <li>Вернуть результат через <code>module.exit_json(changed=True, meta=result)</code> или <code>module.fail_json(msg="Error")</code>.</li>
            </ol>
            <p>Модуль должен быть <strong>идемпотентным</strong>: проверять текущее состояние перед внесением изменений.</p>`,
        tip: "Разместите модуль в папке `library/` рядом с плейбуком или внутри роли, и Ansible подхватит его автоматически."
      },
      {
        q: "Action Plugins vs Modules: в чем разница?",
        a: `<p>Это глубокий архитектурный вопрос.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Modules:</strong> Выполняются <strong>на удаленном хосте</strong>. Делают грязную работу (Restart service, Edit file).</li>
                <li><strong>Action Plugins:</strong> Выполняются <strong>на контроллере (локально)</strong>. Они управляют процессом запуска модуля.</li>
            </ul>
            <p><em>Пример:</em> Модуль <code>template</code> работает так: Action Plugin локально рендерит шаблон Jinja2 в файл, затем вызывает модуль <code>copy</code> (или <code>file</code>) для отправки готового файла на удаленный хост.</p>`,
        tip: "Если вам нужно сделать что-то локально перед отправкой данных на сервер (или вообще не ходить на сервер), пишите Action Plugin."
      }
    ]
  },
  {
    id: 'as3', title: '3. Архитектура и Безопасность', desc: 'Pull mode, Vault, Secret Management',
    questions: [
      {
        q: "Push vs Pull архитектура: как работает ansible-pull?",
        a: `<p>Классический Ansible — это <strong>Push</strong> (центральный сервер ходит по SSH ко всем).</p>
            <p><strong>Ansible-pull</strong> инвертирует модель (Pull):</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li>Ansible устанавливается на <em>каждый</em> управляемый хост.</li>
                <li>Агент (cron) периодически скачивает репозиторий с плейбуками (git clone) и применяет их локально (<code>localhost</code>).</li>
            </ul>
            <p><strong>Плюсы Pull:</strong> Масштабируется до бесконечности (нет бутылочного горлышка на мастере), работает для машин за NAT (которые не могут принять входящий SSH).</p>`,
        tip: "Pull модель отлично подходит для автоскейлинга и конфигурации рабочих станций (laptops), которые часто меняют IP или находятся оффлайн."
      },
      {
        q: "Безопасность секретов: Ansible Vault и `no_log`.",
        a: `<p><strong>Ansible Vault</strong> шифрует файлы (AES256). Можно шифровать как весь файл (<code>vars.yml</code>), так и отдельные строки (<code>ansible-vault encrypt_string</code>).</p>
            <p><strong>Проблема логов:</strong> Даже если переменная зашифрована, Ansible по умолчанию выводит аргументы задачи в лог при выполнении. Если вы передаете пароль в модуль, он может утечь в stdout CI-системы.</p>
            <p><strong>Решение:</strong> Используйте <code>no_log: true</code> на уровне задачи (Task), чтобы подавить вывод чувствительных данных.</p>`,
        tip: "Если `no_log: true` мешает отладке, используйте `loop_control: label`, чтобы скрыть чувствительные поля цикла, но оставить идентификаторы."
      }
    ]
  }
];

export const course: CourseConfig = {
  id: 'ansible',
  title: 'Ansible: IaC',
  description: 'Автоматизация конфигураций, идемпотентность и написание ролей для High Load.',
  levels: {
    junior: {
      id: 'junior',
      title: 'Junior',
      subTitle: 'Playbooks & Inventory',
      icon: '🌱',
      color: 'emerald-500',
      borderColor: 'border-emerald-500',
      bgHover: 'group-hover:bg-emerald-600',
      textHover: 'group-hover:text-emerald-600',
      description: 'Основы синтаксиса YAML, структура плейбуков, инвентарь и базовые модули (yum, service, copy). Понимание идемпотентности.',
      modules: JUNIOR_MODULES
    },
    middle: {
      id: 'middle',
      title: 'Middle',
      subTitle: 'Roles & Logic',
      icon: '⚙️',
      color: 'blue-600',
      borderColor: 'border-blue-600',
      bgHover: 'group-hover:bg-blue-600',
      textHover: 'group-hover:text-blue-600',
      description: 'Создание переиспользуемых Ролей, Jinja2 шаблонизация, управление переменными и обработка ошибок. Оптимизация структуры проекта.',
      modules: MIDDLE_MODULES
    },
    senior: {
      id: 'senior',
      title: 'Senior',
      subTitle: 'Architecture & Plugins',
      icon: '🧠',
      color: 'purple-600',
      borderColor: 'border-purple-600',
      bgHover: 'group-hover:bg-purple-600',
      textHover: 'group-hover:text-purple-600',
      description: 'Глубокое понимание движка (forks, strategies), написание кастомных модулей/плагинов на Python, масштабирование (Pull mode) и безопасность.',
      modules: SENIOR_MODULES
    }
  }
};
