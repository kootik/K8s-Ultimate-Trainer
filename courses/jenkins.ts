
import { CourseConfig } from '../types';

const JUNIOR_MODULES = [
  {
    id: 'jj1', title: '1. Основы Jenkins', desc: 'Architecture, Jobs, Agents',
    questions: [
      {
        q: "Чем отличается Declarative Pipeline от Scripted Pipeline?",
        a: `<p>Это два разных синтаксиса описания пайплайнов в <code>Jenkinsfile</code>.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Declarative (Декларативный):</strong> Начинается с <code>pipeline { ... }</code>. Более жесткая структура, легче читать, встроенная проверка синтаксиса (linting). Это рекомендуемый стандарт. Поддерживает директивы <code>stages</code>, <code>post</code>, <code>environment</code>.</li>
                <li><strong>Scripted (Скриптовый):</strong> Начинается с <code>node { ... }</code>. Это чистый Groovy код. Дает полную свободу и мощь (циклы, сложные условия, try-catch), но сложнее в поддержке и визуализации.</li>
            </ul>`,
        tip: "Если вам не нужна сложная логика ветвления, всегда выбирайте Declarative. Он лучше интегрируется с Blue Ocean (UI) и позволяет делать рестарт с конкретного стейджа."
      },
      {
        q: "Что такое Agent и Executor?",
        a: `<p><strong>Agent (Node/Slave):</strong> Это компьютер (виртуальная машина, контейнер), к которому подключен Jenkins Master для выполнения задач.</p>
            <p><strong>Executor:</strong> Это слот (поток) на агенте. Агент может иметь несколько экзекьюторов (например, 2 или 4), что определяет, сколько задач (Jobs) он может выполнять параллельно.</p>
            <p>Master (Controller) занимается оркестрацией и раздачей задач агентам.</p>`,
        tip: "Никогда не запускайте сборки на Master-ноде (executor count: 0). Master должен заниматься только планированием и управлением, иначе тяжелая сборка может 'уронить' весь Jenkins."
      },
      {
        q: "Как триггерить пайплайн? (Poll SCM vs Webhook)",
        a: `<p>Два способа узнать об изменениях в репозитории:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Poll SCM:</strong> Jenkins сам периодически (по cron-расписанию, напр. <code>H/5 * * * *</code>) опрашивает Git. Это создает лишнюю нагрузку на Git-сервер и задержку запуска.</li>
                <li><strong>Webhook:</strong> Git-сервер (GitHub/GitLab) сам посылает HTTP POST запрос в Jenkins при событии <code>git push</code>. Сборка стартует мгновенно.</li>
            </ul>`,
        tip: "Poll SCM допустим только если Jenkins находится за строгим фаерволом и до него не долетают вебхуки из интернета (GitHub)."
      }
    ]
  },
  {
    id: 'jj2', title: '2. Переменные и Окружение', desc: 'Env Vars, Parameters',
    questions: [
      {
        q: "Как передать параметры в сборку (Build with Parameters)?",
        a: `<p>В Declarative Pipeline используется блок <code>parameters</code> в начале файла.</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
pipeline {
    agent any
    parameters {
        string(name: 'BRANCH', defaultValue: 'main', description: 'Target branch')
        booleanParam(name: 'DEPLOY', defaultValue: false, description: 'Deploy after build?')
    }
    stages { ... }
}
            </div>
            <p>Доступ к ним осуществляется через объект <code>params.BRANCH</code> или просто как к переменной окружения <code>env.BRANCH</code>.</p>`,
        tip: "При первом запуске пайплайна с новым Jenkinsfile параметры не появятся в UI. Нужно запустить сборку один раз 'вслепую', чтобы Jenkins просканировал файл и обновил интерфейс."
      }
    ]
  }
];

const MIDDLE_MODULES = [
  {
    id: 'jm1', title: '1. Продвинутые Пайплайны', desc: 'Shared Libraries, Parallel',
    questions: [
      {
        q: "Зачем нужны Shared Libraries и какова их структура?",
        a: `<p>Shared Libraries позволяют выносить общий код (функции, классы) из Jenkinsfile в отдельный репозиторий, следуя принципу DRY (Don't Repeat Yourself).</p>
            <h4 class="font-bold mt-2">Структура репозитория:</h4>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><code>vars/</code>: Глобальные функции (скрипты), которые вызываются прямо из пайплайна (например, <code>deployToK8s.groovy</code> вызывается как <code>deployToK8s()</code>).</li>
                <li><code>src/</code>: Классы Groovy (пакеты) для сложной объектно-ориентированной логики (<code>com.company.Utils</code>).</li>
                <li><code>resources/</code>: Статические файлы (JSON шаблоны, SQL скрипты), которые загружаются через <code>libraryResource</code>.</li>
            </ul>`,
        tip: "Shared Libraries загружаются в 'Trusted Sandbox'. Это значит, что код внутри библиотеки имеет меньше ограничений безопасности, чем код в самом Jenkinsfile."
      },
      {
        q: "Как работает параллельное выполнение (parallel)?",
        a: `<p>Блок <code>parallel</code> позволяет запускать стейджи одновременно на разных экзекьюторах.</p>
            <div class="bg-slate-800 text-white p-2 rounded text-xs font-mono my-1">
stage('Tests') {
    parallel {
        stage('Unit Tests') { steps { ... } }
        stage('Integration Tests') { steps { ... } }
    }
}
            </div>
            <p>Если один из параллельных этапов упадет, по умолчанию упадет весь пайплайн (<code>failFast false</code>). Можно настроить <code>failFast true</code>, чтобы мгновенно остановить остальные ветки.</p>`,
        tip: "Параллелизм ускоряет сборку, но потребляет больше слотов (executors). Убедитесь, что у вас достаточно агентов, иначе задачи просто встанут в очередь."
      }
    ]
  },
  {
    id: 'jm2', title: '2. Безопасность и Docker', desc: 'Credentials, Docker Agents',
    questions: [
      {
        q: "Как безопасно использовать секреты (Credentials) в пайплайне?",
        a: `<p>Никогда не пишите пароли в Jenkinsfile. Используйте Credentials Binding Plugin.</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Declarative:</strong> Блок <code>environment</code> c хелпером <code>credentials()</code>.
                    <br><code>AWS_KEY = credentials('aws-prod-key')</code></li>
                <li><strong>Scripted:</strong> Блок <code>withCredentials([string(...)]) { ... }</code>.</li>
            </ul>
            <p>Jenkins автоматически маскирует (звездочками <code>****</code>) значения секретов в консольном логе, если они туда попадают.</p>`,
        tip: "Будьте осторожны с shell-скриптами: `sh 'echo $PASSWORD > config'` безопасен (Jenkins скроет), но вывод секрета в файл делает его доступным для чтения в рабочей директории (workspace)."
      },
      {
        q: "Docker in Docker (DinD) vs Docker Socket Binding (DooD) в Jenkins?",
        a: `<p>Когда Jenkins запускает агента в контейнере и ему нужно собирать другие Docker образы, есть два пути:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li><strong>Docker Socket Binding (DooD):</strong> Проброс <code>/var/run/docker.sock</code> с хоста. Контейнер агента использует демон хоста.
                    <br><em>Плюс:</em> Кэш образов общий с хостом (быстро).
                    <br><em>Минус:</em> Агент имеет root-доступ к хосту (безопасность).</li>
                <li><strong>Docker in Docker (DinD):</strong> Запуск привилегированного контейнера с отдельным Docker демоном внутри.
                    <br><em>Плюс:</em> Полная изоляция.
                    <br><em>Минус:</em> Медленно (нет кэша), требует <code>privileged</code> режима, проблемы с файловой системой (overlay-in-overlay).</li>
            </ul>`,
        tip: "Kaniko — современная альтернатива для сборки образов в K8s без демона Docker и без привилегий root."
      }
    ]
  }
];

const SENIOR_MODULES = [
  {
    id: 'js1', title: '1. Архитектура и Масштабирование', desc: 'JCasC, Kubernetes Plugin',
    questions: [
      {
        q: "Что такое Jenkins Configuration as Code (JCasC)?",
        a: `<p>Плагин JCasC позволяет настроить <strong>сам Jenkins Master</strong> через YAML файл, а не прокликивая UI.</p>
            <p>В файле <code>jenkins.yaml</code> описывается:</p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li>Подключение к Clouds (Kubernetes, EC2).</li>
                <li>Настройки безопасности (Security Realm, Authorization).</li>
                <li>Глобальные библиотеки.</li>
                <li>Настройки плагинов.</li>
            </ul>
            <p>При старте Jenkins читает этот файл и применяет настройки. Это делает инсталляцию воспроизводимой и версионируемой (GitOps для Jenkins).</p>`,
        tip: "JCasC не управляет самими Джобами (пайплайнами). Для этого используется Job DSL Plugin или сканирование организаций (Organization Folder)."
      },
      {
        q: "Как работает Kubernetes Plugin для Jenkins (Ephemeral Agents)?",
        a: `<p>Это нативная интеграция для динамического скейлинга.</p>
            <ol class="list-decimal pl-5 mt-2 space-y-1">
                <li>Пайплайн запрашивает агента с меткой <code>label 'k8s'</code>.</li>
                <li>Jenkins Master через API Kubernetes создает <strong>Pod</strong>.</li>
                <li>В Поде запускается контейнер JNLP (агент Jenkins), который соединяется с Мастером.</li>
                <li>Сборка выполняется внутри этого Пода.</li>
                <li>После завершения сборки Под <strong>удаляется</strong>.</li>
            </ol>
            <p><strong>Преимущества:</strong> Чистое окружение для каждой сборки (нет "дрейфа конфигурации"), экономия ресурсов (нет простаивающих агентов).</p>`,
        tip: "Определяйте `yaml` спецификацию пода прямо в Jenkinsfile (через `podTemplate`), чтобы разработчики могли сами управлять требованиями к CPU/RAM и sidecar-контейнерам (например, поднять БД для тестов рядом)."
      }
    ]
  },
  {
    id: 'js2', title: '2. Groovy & Script Security', desc: 'Sandbox, CPS, NonCPS',
    questions: [
      {
        q: "Что такое CPS (Continuation Passing Style) в Jenkins и зачем нужен @NonCPS?",
        a: `<p>Jenkins Pipeline выполняется не как обычный Groovy скрипт. Он компилируется в стиль CPS, чтобы выполнение можно было <strong>приостановить</strong> (например, на <code>input</code> или ожидании агента) и продолжить после перезагрузки Мастера.</p>
            <p><strong>Проблема:</strong> Не весь Groovy код сериализуем. Если вы используете сложные циклы (например, итераторы C-style) или несериализуемые объекты внутри скрипта, Jenkins может упасть с ошибкой <code>NotSerializableException</code>.</p>
            <p><strong>@NonCPS:</strong> Эта аннотация перед функцией говорит Jenkins: "Выполняй это как обычный Groovy, без CPS трансформации". Это быстрее и позволяет использовать всю мощь Groovy, но внутри такой функции <strong>нельзя</strong> вызывать шаги пайплайна (например, <code>sh</code> или <code>sleep</code>).</p>`,
        tip: "Используйте @NonCPS для сложной логики парсинга JSON/XML или математических вычислений, которые должны пройти мгновенно."
      },
      {
        q: "Как работает Script Security (Sandbox) и Approval?",
        a: `<p>Jenkins запускает Pipeline (особенно из недоверенных SCM) в <strong>Groovy Sandbox</strong>.</p>
            <p>Песочница разрешает только безопасный набор методов (Whitelist). Если скрипт попытается сделать <code>System.exit(0)</code> или прочитать файл через <code>new File()</code> на мастере, выполнение заблокируется с ошибкой <code>ScriptSecurityException</code>.</p>
            <p><strong>Решения:</strong></p>
            <ul class="list-disc pl-5 mt-2 space-y-1">
                <li>Администратор может вручную разрешить метод в меню "In-process Script Approval" (плохо для масштабируемости).</li>
                <li>Вынести опасный код в <strong>Shared Library</strong>. Код библиотек считается доверенным и выполняется вне песочницы.</li>
            </ul>`,
        tip: "Никогда не отключайте Sandbox глобально. Это позволит любому разработчику с доступом к Git выполнить произвольный код на сервере Jenkins (RCE)."
      }
    ]
  }
];

export const course: CourseConfig = {
  id: 'jenkins',
  title: 'Jenkins CI/CD',
  description: 'Автоматизация сборок: от простых Freestyle задач до масштабируемых пайплайнов в Kubernetes.',
  levels: {
    junior: {
      id: 'junior',
      title: 'Junior',
      subTitle: 'Pipeline Basics',
      icon: '🏗️',
      color: 'emerald-500',
      borderColor: 'border-emerald-500',
      bgHover: 'group-hover:bg-emerald-600',
      textHover: 'group-hover:text-emerald-600',
      description: 'Основы CI/CD, синтаксис Declarative Pipeline, управление агентами и триггерами.',
      modules: JUNIOR_MODULES
    },
    middle: {
      id: 'middle',
      title: 'Middle',
      subTitle: 'Libraries & Docker',
      icon: '⚙️',
      color: 'blue-600',
      borderColor: 'border-blue-600',
      bgHover: 'group-hover:bg-blue-600',
      textHover: 'group-hover:text-blue-600',
      description: 'Написание Shared Libraries (Groovy), параллельное выполнение, интеграция с Docker и управление секретами.',
      modules: MIDDLE_MODULES
    },
    senior: {
      id: 'senior',
      title: 'Senior',
      subTitle: 'JCasC & Scale',
      icon: '🚀',
      color: 'purple-600',
      borderColor: 'border-purple-600',
      bgHover: 'group-hover:bg-purple-600',
      textHover: 'group-hover:text-purple-600',
      description: 'Configuration as Code, динамические агенты в Kubernetes, безопасность скриптов (CPS, Sandbox) и администрирование.',
      modules: SENIOR_MODULES
    }
  }
};
